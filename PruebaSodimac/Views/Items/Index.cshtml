@model IEnumerable<PruebaSodimac.Models.Item>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipoitem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TiposervicioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipoitem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TiposervicioNavigation.Rowidtiposervicio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRepuesto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRepuesto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRepuesto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@model IEnumerable<PruebaSodimac.Models.Preciosrepuesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fechavigencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodrepuestoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fechavigencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodrepuestoNavigation.IdRepuesto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Rowidprecios">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Rowidprecios">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Rowidprecios">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
